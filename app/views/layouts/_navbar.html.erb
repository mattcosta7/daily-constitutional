<header class="mdl-layout__header">
  <div class="mdl-layout__header-row">
    <div class="mdl-layout-spacer"></div>
    <nav>
      <div class="mdl-textfield mdl-js-textfield mdl-textfield--align-left">
        <%=link_to 'Feed', user_path(current_user), class: 'nav-link' if current_user%>
        <%=link_to 'Starred', favorites_path(current_user), class: 'nav-link' if current_user%>
        <%=link_to 'Sign Out', login_path, method: :delete, class: 'nav-link', data: {confirm: "Don't Go!"}  if current_user %>
      </div>
    </nav>
  </div>
</header>
<%=render 'layouts/drawer'%>