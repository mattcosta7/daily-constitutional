<%=render @user%>
<%=link_to "Edit", edit_user_path(current_user) if @user == current_user%>
<%=render 'partials/weather'%>
<%=render 'partials/mta' if @tStatus %>
<%=render 'partials/todos' if @user.todos.length > 0%>