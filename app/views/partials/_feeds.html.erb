<ul>
  <h5>Feeds </h5>
  <%=collection_select(:category_selector, :category_id, current_user.categories.order(:title).all, :title, :title, {prompt: "Filter By Category", selected: (@category ? @category.title : nil)})%>

  <%=collection_select(:blog_selector, :blog_id, current_user.blogs.all, :id, :url, {prompt: "Filer By Blog",selected: (@blog ? @blog.url : nil)})%>


</ul>


